import { Button, Container, Typography } from "@mui/material";

function App() {
  return (
    <Container>
     <Typography sx={{fontSize: "2rem"}}>Hello World!</Typography>
     <Button variant="contained" color="primary">Click</Button>
    </Container>
  )
}

export default App
